#include "oinc.h"
M_control init()
{
        uint64_t PC = 0;
        uint8_t cont;
        uint8_t *reg;
        reg = realloc()
        uint8_t *mem;
        return {PC,cont,reg,mem};
}
int boot(M_control mc)
{
        return 0;
}
int exec_instruction(M_instru mi) 
{
    return 0;
}
